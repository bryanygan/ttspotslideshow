spotipy==2.24.0
python-dotenv==1.0.1
Pillow==10.4.0
# Optional: for video export
imageio[ffmpeg]
# Optional: cross-platform OCR fallback (needs the Tesseract binary on PATH).
# Only required on non-Windows hosts; Windows uses the built-in WinRT OCR engine.
pytesseract
